services:
  vpn-gateway:
    image: linuxserver/wireguard
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    volumes:
      - ./server.conf:/config/wg0.conf
    ports:
      - "51820:51820/udp"
    sysctls:
      - net.ipv4.ip_forward=1
